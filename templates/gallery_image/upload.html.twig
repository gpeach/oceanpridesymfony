{% extends 'base.html.twig' %}

{% block body %}
    {{ form_start(form) }}
    {{ form_row(form.file) }}
    {{ form_row(form.name) }}
    <button type="submit">Upload</button>
    {{ form_end(form) }}
{% endblock %}
